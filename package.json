{
  "name": "stayfinder-ai",
  "private": true,
  "type": "module",
  "description": "AI-powered accommodation concierge — static frontend + a Vercel Edge Function proxy that talks to Claude.",
  "dependencies": {
    "@upstash/ratelimit": "^2.0.5",
    "@upstash/redis": "^1.34.3"
  }
}
